apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flink-operator.fullname" . }}-test-connection"
  labels:
{{ include "flink-operator.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "flink-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
